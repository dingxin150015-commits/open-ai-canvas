# 路线 B：固定 `4ba9694` 本地候选保留卷升级

## 当前状态

- 执行日期：2026-09-01。
- 候选源码：远程检查点 `e02cee1` 加本地部署标识提交 `c3f5479`。
- UI 构建标识：`v1.1.4+local.4ba.e02cee1`；该标识不表示已跟上官方实时主干。
- 当前运行镜像：
  - Backend `sha256:8182cc0a88cf2a02df30749f25ceb6aac7e23a3ec4a50ae787f4b70570c12095`
  - Web `sha256:522109d7e69f0ece1ea6c7d48440bd45dfde81be0cff6d9821bd200cb8347d10`
- 当前容器：Backend `bbe85006b5df`，Web `abbc9af6205f`；均 running/healthy、RestartCount=0、OOM=false。
- 真实数据卷保持 `open-ai-canvas_backend-data:/data`，没有删除或替换。
- 当前停在 Microsoft Edge 人工验收之前；没有真实模型调用、OSS 删除或候选 Edge 证据。

## 恢复点

- ID：`BKP-20260901-114427-ROUTE-B-PREDEPLOY`。
- 状态：`verified`、`protected`、`pre-route-b`。
- 私有位置：`<private-backup-root>\route-b-predeploy-20260901-114427`。
- 内容：完整 `/data` 副本、逐文件冻结的 DB/WAL/SHM/`.settings-key`/迁移标记、独立恢复数据库、Git bundle、两次克隆迁移结果和升级后真实卷快照。
- 升级前原始哈希：
  - DB `8A9FEFB38E0478B3947F93E58DFAA8EB5967239D08CD108C099E3EF82950157B`
  - WAL `640C18769A0F39CABF4E9E844529F235386E9BFA1DBA2CCE85543885E63CD3E8`
  - SHM `773E8F51D9595C9C68906ED1E0C63A538376C79D085AF2CC0258C0580B59B5A6`
  - `.settings-key` `E4B4B107C3060A18D65AD3AAD4E329D063A924A71CCF23EAB15B9ABF8120A6D5`
  - 迁移标记 `0435B398CEC770046D79B55E9C2E6AB06D236A7759FBB44503266E1DFEA0C8F3`
- 升级前独立恢复：完整性 `ok`、外键 0、60 张表；用户/渠道/模型/任务/资源/素材/画布为 1/1/320/3/5/5/1。
- 逐文件 ACL 无当前用户、SYSTEM、Administrators之外的主体；Git bundle完整可验证。
- 回滚镜像：
  - `open-ai-canvas-backend:pre-route-b-20260901-114427` → `sha256:f94f555b...`
  - `open-ai-canvas-web:pre-route-b-20260901-114427` → `sha256:31417063...`

## 克隆数据迁移演练

- 使用独立命名卷、禁网候选 Backend，未连接真实卷。
- 第一次启动：healthy、迁移错误 0；数据库从 60 增至 72 张表。
- 新增/确认：`skill_versions`、`skill_files`、项目默认图片/视频模型、章节 `word_count`；规范化 skill 包 33 个。
- 第二次启动：healthy、累计迁移错误 0；表数及所有业务计数与第一次完全一致。
- 两次结果：完整性 `ok`、外键 0；skills/versions/files均为33，用户/渠道/模型/任务/资源/素材/画布保持1/1/320/3/5/5/1；`.settings-key`不变。
- 演练容器和克隆卷已删除；私有验证证据保留。真实卷未在演练中挂载。

## 真实保留卷升级结果

- 旧镜像先增加回滚标签，再将候选切换到 `:local`。
- 只执行 `docker compose -f docker-compose.local.yml up -d --no-build --force-recreate`；未执行 `down` 或任何 volume删除。
- 新 Backend/Web容器均 healthy，HTTP `/api/health` 返回业务 `code=0/status=ok`。
- Web `index.html` SHA-256为 `AD03BEAD202160E14100D968F152127DCC6AA4B33B4E4E068F71003E3C1F076E`，入口为 `assets/index-B8g7wkDu.js`；版本资源包含 `v1.1.4+local.4ba.e02cee1`。
- Backend/Web最近启动日志中 panic/fatal、迁移失败和Web error均为0。
- 升级后真实卷快照：完整性 `ok`、外键0、72张表；关键Schema和业务计数与演练一致；`.settings-key`与迁移标记哈希未变化。

## Microsoft Edge 人工验收清单

验收只使用已登录 Microsoft Edge，访问 `http://localhost:3000`；不要改用Chrome。全程不要点击生成、模型测试、Skill自动更新、删除、批量操作、保存存储配置或任何可能产生费用/外部写入的动作。

### 1. 版本与会话

1. 使用 `Ctrl+F5` 强制刷新。
2. 左下角必须显示 `v1.1.4+local.4ba.e02cee1`。
3. 确认原登录会话仍有效；若跳转登录页，只使用既有账号登录，不注册新账号。
4. 刷新一次，确认会话不丢失、没有循环跳转或白屏。

### 2. 首页与全局导航

1. 首页应保持原稳定工作台，不应出现被暂缓的统计仪表盘版本。
2. 依次打开项目、自由画布、素材库、任务中心、技能库和设置，再返回首页。
3. 确认侧栏、顶栏、滚动、返回和响应式布局正常，无持续全屏Loading。
4. 切换浅色/深色主题；浅色画布背景应为柔和灰色，文字和节点保持可读。

### 3. 项目与短剧工作台

1. 项目列表应能看到原有项目，数量和封面不丢失。
2. 打开现有项目，检查概览、章节、资产、画布和设置标签均可进入。
3. 章节列表加载后滚动并切换章节，确认没有一次性加载卡死或正文丢失。
4. 打开项目设置，确认“默认生图模型/默认视频模型”控件存在；只查看，不保存。
5. 进入分镜工作台，检查：
   - 左侧资产、章节和镜头切换可用；长资产名不造成页面横向滚动。
   - 镜头脚本、画面提示、动作/视频提示、预览和底部镜头轨道完整。
   - 任务恢复状态、历史产物、删除镜头按钮和取消资产引用入口存在；不要点击删除或解绑。
   - 生成模型、时长、画幅、分辨率/画质、预计积分和Skill选择器可见；不要点击生成。
   - 已绑定角色/场景/道具显示正确，提示词输入 `@` 时菜单能显示已绑定资产；输入后不要保存，可刷新页面放弃编辑。

### 4. 素材、任务与技能

1. 素材库应显示原有5项素材；图片/视频预览可打开，不应出现资源404或跨账号内容。
2. 任务中心应显示原有3条任务，状态、时间和已有产物可读；不要重试或删除。
3. 技能库应显示33个内置Skills；打开任意详情，确认 `SKILL.md`和文件列表可读取。
4. 不执行安装、编辑、GitHub同步或自动更新。

### 5. 管理后台

1. 打开数据概览，确认用户、渠道、模型、任务和资源统计可读取。
2. 打开用户管理、系统渠道、逻辑模型、请求日志、公告、功能开放、存储服务、运行策略和技能/插件页面。
3. 系统渠道应保留1个渠道、320个模型；Ready/Planned状态和价格/能力页面不应空白或死锁。
4. 存储设置应正常加载既有配置；不要点击保存、连接测试或删除。
5. 公告编辑、模型价格抽屉、资源详情Drawer应能打开和关闭，但不要提交任何修改。

### 6. 控制台与网络

1. 按 `F12` 打开DevTools Console，清空后依次访问上述页面。
2. 不应出现未捕获异常、React渲染崩溃、无限请求或持续401/403/404/500。
3. Network中确认 `/api/health` 返回HTTP 200且业务 `code=0`。
4. 重点观察 `/api/projects`、项目分页/工作区、`/api/tasks`、`/api/skills`、资源接口和管理后台接口。
5. 保存以下截图：版本号、首页、项目概览、分镜工作台、素材库、任务中心、技能详情、后台数据概览、Console无错误和Network健康请求。

### 7. 停止条件

出现以下任一情况立即停止，不进行生成或保存，并把页面、URL、时间、Console和Network截图发回：

- 登录循环、白屏或持续Loading；
- 原项目/素材/任务丢失或数量异常；
- 资源403/404、跨账号数据、数据库/迁移错误；
- 分镜工作台字段、资产引用、任务恢复或历史产物缺失；
- 管理后台模型/价格/存储页面空白、死锁或保存按钮自行触发；
- 版本不是 `v1.1.4+local.4ba.e02cee1`；
- Console未捕获异常或关键API 5xx。

人工验收完成前，本部署状态为 `runtime_candidate_healthy / edge_pending`，不能升级为已验收发布。
