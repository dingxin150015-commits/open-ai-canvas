# v1.2.7 阶段9稳定晋升记录

## 结论

- 用户已在现有登录态 Microsoft Edge 完成当前生产环境可安全观察的版本、导航、创作入口、项目/自由画布、明暗主题与网格、响应式布局、DevTools、Runtime探测、报价和分镜等复验，反馈暂未发现问题。
- Director F6“远端保存失败后选择留在导演台”不能在生产环境安全、可控地构造失败前提，因此单列为测试环境门禁；它既不记为通过，也不再把生产环境可观察项误记为失败。
- 功能检查点为 `37bb237583c8705939ddfa682c00f26677f43f57`。候选与稳定分支均通过 dry-run 后使用非强制快进推送到该检查点，未改动 `origin/main`、默认分支设置、标签、Release 或 PR。
- 本记录随最终纯文档提交发布；最终分支 SHA 以 Git 引用复核为准，不把自引用 SHA 写入提交正文。

## 发布与运行证据

- 固定官方基线：`v1.2.7@371bc68`，是功能检查点祖先。
- 晋升前稳定基线：`5c1203eaf4876b697a4d89236f4979c03b4e34f5`，是功能检查点祖先；因此稳定晋升为快进。
- 用户 Fork：`origin/codex/upstream-v1.2.7-20260908` 与 `origin/codex/dingxin-stable` 已在功能检查点首次对齐；`origin/main` 保持 `11931d0085ccfec3952a4a7e30bd482173c51492`。
- Fork 默认分支仍为 `codex/dingxin-stable`；这里只推进其内容，没有修改仓库默认分支配置。
- 运行 Web 身份：`v1.2.7+dingxin.1 / 0d815d15581505b36e264d6b1f4b44f63acafab8`；Backend 身份：`v1.2.7+dingxin.1 / ae01c5d667a979c835da03bca21bc4d418382e4e`。二者 healthy、RestartCount=0；稳定分支尾部仅增加源码和文档记录，不代表运行容器自动跟随 Git 分支。
- 阶段8恢复点 `BKP-20260909-005845-V127-FOLLOWUP` 保持 `verified/protected`；阶段9没有迁移数据库、替换卷或调用 Provider。

## 验证边界

- Edge结论来自用户对当前生产环境可正常进入且无需故障注入的路径的人工验证；不外推为真实付费生成、Provider账号、OSS新写入、强写路径或全部浏览器组合已通过。
- F6需在隔离测试环境使用确定性的保存失败夹具后，由 Microsoft Edge 验证“留在导演台”对话框关闭且导演台保持打开；生产环境不通过破坏网络、凭据或数据来制造失败。
- `.github/workflows/quality.yml` 只监听 `main` push/PR；本次候选与稳定分支 push 不会触发该工作流，远程CI状态记为“未触发”，不是“通过”。本地全量门禁证据沿用阶段6、阶段8及后续修复记录。
- 官方 `main` 已前进到 `83d9622fd07869c5d55a5735744df1d0e5b84a71`；本阶段仍严格以固定 `v1.2.7@371bc68` 为上游边界，新增尾差未合入，应另做只读审计。
- Host Updater继续保持不可用/未启用，不能用官方默认来源覆盖用户 Fork 的稳定能力。

## 后续顺序

1. 在隔离测试环境设计并执行 F6 的确定性失败场景，保留 Edge 证据；未完成前继续标记 `test_only_pending`。
2. 对 `371bc68..83d9622` 做新的只读上游尾差和冲突审计，再决定下一次增量合并范围。
3. v1.2.7主线稳定后，另行评估 Astravia 作为次级工作台；不直接访问生产数据库、密钥或稳定 checkout。
