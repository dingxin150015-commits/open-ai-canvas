# v1.2.7阶段5：选择性UI适配

用户已批准阶段5。本轮基点为8f730ac4，候选codex/upstream-v1.2.7-20260908。阶段5源码适配、验证与本地提交完成并停止；未push、未部署、未接触真实数据或操作用户Edge。视觉验收仍待后续阶段，不能由源码门禁替代。

## 选择表与新提交

- e027aa4：部分适配首页/创作入口、工作台偏好与临时画布主题隔离、自定义外观传入渲染；保留官方固定屏幕网格，移除重复透明度。
- f5fadb7：不整包重放失败UI2布局。保留素材图标与数量分离、手动专注和可访问侧栏；按实际画布宽度统一同一行控件，次要操作进入更多菜单，顶栏采用紧凑入口。宽度/碰撞仍待用户Edge验收。
- bcb6c6d：部分适配统一报价及错误显示、参数不偷偷取整；优先逻辑模型精确能力配置校验完整报价意图，持久渠道能力为次级依据，缺元数据时不以通用默认冒充真实能力。Runtime改为共享单次自动发现、并发连接合并及有界SSE重连，手动连接保留。
- b6254b4历史记忆不整包覆盖当前候选状态。

| 来源 | 新提交 | 处理 |
| --- | --- | --- |
| e027aa4d34a30fb2e8243e1f90944a18e0f07f9e | 8082e9e2 | 部分适配；保留官方网格，导航/主题/真实渲染接入，额外覆盖分享画布清理 |
| f5fadb76579bc44ef7b8c6cec25e263b245b6eff | 2aebd53e | 部分适配；拒绝撑满Dock/原生滚动条方案，在原控件上增加实际宽度与更多入口 |
| bcb6c6d09d2c16af2dfba223a9b3e4d17eec0642 | d10c5e32 | 部分适配；替代仅通用时长预检和单组件挂载判断，补真实逻辑能力与共享连接生命周期 |

project.tsx同时包含主题和布局，按已审阅的主题区块单独暂存，余下布局进入第二提交；未覆盖工作树。每个提交正文有Adapted-from来源说明，没有整包cherry-pick旧UI2。

## 验证结果

- 干净TypeScript通过（incremental=false）。
- 默认Web测试链 `27+4+16+1174+33+1+23=1278 pass / 0 fail`。新UI适配专项23项另行独立通过；不重复计入默认链总数。保留panic guard、跨Runtime及旧连接结果保护回归。
- 生产构建通过，16.98秒。测试镜像 `open-ai-canvas-web-test:v127-stage5`，manifest list `sha256:83d07de4d109d80caae1b85dfa1b6bbb89d5616793fb344d574a306ffcc7a348`，未部署。
- 候选工作区33个改动Web文件与测试镜像逐文件SHA256一致；diff检查通过。本轮没有改动Backend/Canvas Agent，因此未重复运行其全量；最终阶段6仍需按最终组合完整验证。
- 日志位于主工作区 `.local/audits/v127-20260908-110746/stage5-second.log`；首次失败日志保留。首轮发现自动加入连接与显式重连语义冲突，现用ensureConnected与connect区分：自动调用不互相取消，用户显式重连仍可替代旧请求，过期结果不能覆盖新结果。
- 旧分镜测试从quoteLogicalModel改为统一quoteModelCatalog断言，保持参考音频与完整意图保护。未取消失败测试，也未放宽类型/价格/能力限制。

## 仍待实际验收

- 1100/760/480/300等实际画布宽度下的控件清晰度、侧栏/DevTools/Agent列组合、更多菜单键盘交互和浮层边界；纯函数/源码测试不等于像素验证。
- 首页/创作独立入口、全局浅色偏好与已有画布独立外观、自定义实时渲染、点/线/空白网格。
- Runtime离线自动发现只一次、SSE重连预算耗尽后停下、启动本机服务后的手动重连。
- 按真实模型声明验证合法/非法时长、分辨率与参考组合；不能因前置校验通过保证后端价格/路由已配置或Provider真实可用。
- 用户此前描述的偶发页面加载失败未在浏览器复现，本阶段不宣称根因已查清或已经关闭。

当前状态 `v127_stage5_source_complete_edge_pending`。阶段6及以后待批准；原UI/stable分支与运行环境不随本地候选提交变化。
